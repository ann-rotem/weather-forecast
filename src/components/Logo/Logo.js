import * as S from "./Logo.style";

const Logo = () => {
	return (
		<S.LogoWrapper>
			<S.LogoText>
				<span>Weather</span> Forecast
			</S.LogoText>
		</S.LogoWrapper>
	);
};

export default Logo;
